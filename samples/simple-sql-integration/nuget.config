<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <!-- Clear all sources to ensure only our specified sources are used -->
    <clear />
    <!-- Local package output from the build (nupkg files) -->
    <add key="local" value="../../src/JD.Efcpt.Build/bin/Debug/" />
    <!-- Fallback to nuget.org for other dependencies -->
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
  </packageSources>
</configuration>
